# before-buy

투자 아이디어 검증기. 종목을 사기 전에 "왜 담았는지"를 기록하고, 그 근거가 여전히 유효한지 되비추는 프로토타입.

## Language

**관심종목**:
아직 사지 않았지만 지켜보는 종목. `watchlist_items.status = watching`. 이 제품의 주 무대.
_Avoid_: 워치리스트, 관심주

**보유중**:
이미 매수한 종목. `watchlist_items.status = bought`. 전제 추적이 계속되며, 상세(S5)에서 **추가 매수와 매도를 양방향으로 열어둔다** — 권유가 아니라 중립 실행이다(ADR-0009). 방향을 고르는 순간(단독 `매도 검토` 같은) 이 제품은 추천하는 물건이 되므로, 두 길은 항상 같은 폭·같은 위계로 나란히 선다.
관심종목에서 빠지는 길은 없다 — 목록에서 지우면 손익 추적이 통째로 사라진다.
_Avoid_: 보유종목(화면 타이틀에는 쓰되 상태값 자체는 "보유중"), 포트폴리오

**근거**:
사용자가 종목을 담을 때 답한 이유(카테고리 + 후속 질문 답변 + 자유 서술). 코드에서는 `thesis`. 사용자 화면 문구에는 "근거"만 쓰고 `thesis`라는 말을 노출하지 않는다.
_Avoid_: 투자근거(배지 등 화면에서는 무겁다), 논리, thesis(사용자 대상 문구에서)

**근거 갱신**:
이미 담긴 종목의 근거를 다시 쓰는 행위. S5 `생각 업데이트 하기`와 S4 주문 전 확인이 그 길이며, 둘 다 S2로 되돌아가 S3 `이대로 담기`로 끝난다.
**결과는 새 관심종목이 아니다** — 같은 관심종목 아래 근거가 한 겹 더 쌓이고, 화면은 언제나 가장 최근 것만 본다. 관심종목 1건 = `watchlist_items` 1행이라는 모델이 여기서 지켜진다(#98).
**담은 날 가격과 담은 시각은 바뀌지 않는다.** 근거를 고쳐 쓴 것은 오늘의 일이지만 담은 것은 그날의 사실이고, 여기가 흔들리면 "담은 날 대비"(#86)가 조용히 오늘 기준으로 리셋된다. `보유중`인 종목의 근거를 갱신해도 상태와 매수 단가는 그대로다.
옛 근거는 지워지지 않고 이력으로 남지만, **사용자에게 보여주지 않는다** — 화면 문구에 `버전`/`version`을 노출하지 않는다. 사용자가 아는 것은 "근거를 다시 썼다"뿐이다.
**S2는 이전 근거를 이어받지 않는다 — 빈 화면에서 다시 시작하며, 그것이 의도다**(#154). 옛 카테고리도 후속 질문 답변도 자유 서술도 채워 넣지 않는다. 근거 갱신은 입력을 아끼는 편의 기능이 아니라 **재고(再考) 장치**이고, 채워진 답이 있으면 사용자는 그것을 읽고 넘기지 다시 생각하지 않는다. 통과가 쉬워지면 거의 같은 입력에 유상 호출만 한 번 더 나가고, `order_events`의 `update_thesis`가 재는 "다시 생각했다"도 묽어진다. 이어받기를 넣자는 제안은 이 마찰이 비용이 아니라 기능이라는 점을 먼저 뒤집어야 한다.
**내용이 같은 근거도 한 겹으로 쌓인다.** `version`은 근거의 스냅샷이 아니라 "언제 다시 생각했나"의 기록이라, 지난번과 똑같이 써 내도 막지 않는다. 시세가 그사이 움직였으므로 같은 근거에 대한 판정은 실제로 달라질 수 있고 — 전제가 깨져 돌아오는 그 장면이 이 제품이 보여주려는 것이다.
_Avoid_: 근거 수정(옛 것을 덮어쓰는 것처럼 들린다), 재담기(관심종목이 하나 더 생기는 것처럼 들린다), 버전 업

**확인해볼 질문**:
근거를 읽은 LLM이 되돌려주는, 아직 답이 없는 질문(`critique.openQuestions`). 두 화면에서 뜻이 다르다 — 담기 직전(S3)에는 `이대로 담기`냐 `다시 쓰기`냐를 가르는 재료이고, 담은 뒤(S5)에는 **아직 아무도 답하지 않은 것으로 남아 근거 갱신을 부르는 자리**다. 그래서 S5에서는 `전제별 상태`와 `생각 업데이트 하기` 사이에 선다.
**답을 받는 전용 입력 경로가 없다.** 답은 다음 근거의 후속 질문·자유 서술로 들어온다 — S2의 후속 질문은 고정 선택지 칩 + 무선택(`skipped`) 문법이라, 여기에 자유 문항을 얹으면 `건너뛰기`가 두 뜻을 갖는다(#96). 목표가·손절가를 하나도 주지 않아 자동 확인 전제를 만들지 못한 근거에 숫자가 들어올 길이 이것이다(프롬프트 명세 4-3).
**화면은 최신 회차의 질문만 본다** — `근거 갱신`과 같은 규율이다. 옛 회차를 쌓거나 "해소됨"을 표시하지 않는다: 해소를 판정할 주체가 없어 새로운 사용자 상태를 하나 더 만들게 된다.
_Avoid_: open_questions(사용자 대상 문구에서), 열린 질문

**담은 날 대비 / 손익**:
S5 `가격 흐름`이 보여주는 **기준이 다른 두 수익률**. `담은 날 대비`는 담은 날 가격 기준이고 `손익`은 매수가 기준이다. 담은 시점과 산 시점이 다를 수 있으므로 하나만 보여주면 둘 중 하나를 오해한다(#86). 코드에서는 `returnSinceAdded` / `returnSinceBuy`.
**두 이름을 대칭으로 맞추지 않는다.** `손익`을 `매수 대비`로 개명하면 둘이 같은 종류의 퍼센트 두 개로 납작해지는데, `담은 날 대비`는 *생각이 아직 맞나*를 재는 진단값이고 `손익`은 *번 돈과 잃은 돈*이라는 사실이다. 어느 값이 어느 시점에서 왔는지는 이름이 아니라 표의 행 순서가 말한다(담은 날 → 매수 → 오늘 → 담은 날 대비 → 손익).
**기준 가격이 없으면 `null`이며, 0%로 접지 않는다** — 지어낸 0은 "안 변했다"라는 거짓말이 된다. 화면은 그 자리를 `-`로 말한다.
_Avoid_: 근거 대비(화면에서만 쓰이던 옛 이름 — 코드·문서는 줄곧 `담은 날 대비`였고, 한 개념의 두 이름을 잇느라 번역 주석까지 달려 있었다 #127), 매수 대비, 수익률(둘을 뭉갠다), 전일 대비(S1 목록 행의 등락률이지 이 둘이 아니다)

**가격 흐름**:
S5 상단에서 담은 날 → (매수) → 오늘을 **시점마다 한 행씩** 세우는 표. 점선 위는 관측된 가격, 아래는 그것으로 계산해낸 `담은 날 대비`·`손익`이다.
**세 번째 시점의 이름은 `오늘`이고, 그 행의 날짜 칸은 비어 있다.** 라벨과 날짜에 `현재`·`오늘`을 나눠 담으면 두 칸이 같은 말을 두 번 해 이 표의 이점인 세로 스캔선 하나가 죽는다(#132). 날짜를 채우지 않는 이유는 **데모에 시계가 없다**는 것이다 — `3개월 후`는 시세 변종일 뿐 담은 날짜는 그대로라, 그 모드의 '오늘'에 해당하는 날짜가 존재하지 않는다. 없는 값을 지어내느니 칸을 비운다.
**`3개월 후` 모드에서도 이 행은 `오늘`이라고 쓴다.** 시뮬레이션은 앱 전체가 하는 것이고 홈 헤더의 배너가 이미 선언한다 — 행마다 `3개월 후`를 붙이면 서비스 화면이 자기가 데모라는 것을 매번 실토하는 꼴이 된다. 데모의 픽션 안에서 '오늘'은 3개월 뒤의 오늘이 맞다.
**시세가 아직 없어도 행은 사라지지 않는다.** 값 자리만 스켈레톤이나 `-`가 되며, 행을 통째로 지우면 시세가 도착할 때 카드 높이가 튄다.
_Avoid_: 가격 정보(화면에 떠도 아무것도 말해주지 않는다), 가격 3단 표시(관심종목은 2단이라 이름이 절반의 경우에만 맞다), 현재(**가격 흐름의 행 이름으로만** 금지다 #132 — `데모 시점`의 값 `현재`와 `현재가`·`현재 시세`는 그대로 쓴다)

**전제**:
근거에서 도출되어 자동/수동으로 계속 확인되는 개별 조건(예: "PER 15배 이하 유지", "목표가 210,000원"). 코드에서는 `premise`.
전제는 **유지 조건**이거나 **도달 목표**이며, 둘은 판정도 다르고 배지에 미치는 영향도 다르다.
_Avoid_: premise(사용자 대상 문구에서), 조건, 가정

**유지 조건**:
지금 참이어야 하는 전제. 깨지면 생각이 틀어진 것이다. **손절선**(가격 하한 — 아래로 내려가면 깨짐)과 **저평가선**(상한 — PER·PBR 또는 가격, 위로 넘으면 깨짐) 두 종류. 배지 상태 **달라짐**을 만드는 것은 오직 이쪽이다.
_Avoid_: 조건(전제와 구별이 안 된다), invariant

**도달 목표**:
아직 오지 않은 기대. **목표가** 하나뿐이며 `도달 전` / `도달함`으로만 말한다. 미도달은 생각이 틀어진 게 아니라 진행 중인 것이므로 **배지에 투표하지 않는다** — 도달해도 마찬가지다. 유지 조건의 어휘(`유효`/`깨짐`)를 빌려 쓰지 않는다: 미도달을 "유효"로 두면 거짓말이고 "깨짐"으로 두면 멀쩡한 종목이 **달라짐**으로 뜬다(#85).
_Avoid_: 목표 전제, 상승 전제

**기준선의 종류**:
자동 확인 전제가 시세와 어느 방향으로 비교되는지는 **종류가 결정한다**. 기록하는 쪽(LLM·시드)은 종류만 말하고 방향(`lte`/`gte`)을 고르지 않는다 — 방향을 고를 수 있게 두면 "손절선인데 비교는 반대"인 조합이 아무 저항 없이 통과한다. 코드에서는 `PremiseCheckConfig.kind`, ADR-0007.
_Avoid_: operator(방향을 고르는 값처럼 들린다), 비교 연산자

**건너뛰기**:
근거를 쓰지 않고 종목만 담는 행위(S2 Step 1). 결과는 **근거 없음** 배지이며, 담은 날 가격은 그대로 기록된다 — 근거와 무관한 사실이라 여기서 놓치면 나중에 근거를 써도 복구되지 않는다.
**후속 질문을 답하지 않는 것과 다르다.** 그쪽은 별도 행위가 아니라 **무선택**으로 표현되며(선택지 칩은 토글이다), 저장될 때 `skipped`로 채워진다. 한 화면에서 같은 말이 두 뜻을 갖지 않도록, 질문 옆의 건너뛰기 버튼은 두지 않는다(#96).
_Avoid_: 나중에 쓰기, 근거 없이 담기(버튼 문구로는 배지의 낙인을 미리 찍는다), 스킵

**직접 입력**:
후속 질문에서 선택지 대신 값을 **사용자가 적어 넣은 답**. 화면에서는 `직접 입력` 칩을 고르면 열리는 입력창이다.
**자동 확인 전제의 숫자가 제품에 들어오는 유일한 문법이다** — 목표가·손절가 문항의 선택지는 `직접 입력`과 `정하지 않음` 둘뿐이라, 사용자가 기준선을 주려면 여기를 지나야 한다. 그래서 이 답이 비면 그 종목은 직접 확인 전제만 갖게 된다.
**적힌 값은 다듬지 않는다.** 사용자가 쓴 문자열이 그대로 근거에 담기고 그대로 모델에게 간다 — `285000`도 `28만 5천원`도 `+15%`도 온다. 값을 숫자로 바꾸는 것은 근거를 읽는 LLM의 일이지 입력창의 일이 아니다(#159 실측: 네 형태 모두 정확히 추출).
답을 프롬프트·화면 문자열로 어떻게 펴는지는 도메인이 아니라 형식이다 — `프롬프트 명세.md` 3장이 갖는다.
_Avoid_: 커스텀, 기타 입력, 자유 입력(선택지가 아예 없는 문항의 답은 이것과 다르다)

**배지 상태**:
관심종목 목록의 행에 표시되는 3상태. **근거 없음**(근거를 안 쓰고 담음) / **유지 중**(전제 모두 유효) / **달라짐**(전제 중 하나 이상 깨짐). 상태는 정확히 이 3개뿐이며 추가하지 않는다.
**달라짐**에 투표할 수 있는 것은 **유지 조건**뿐이다 — 도달 목표는 어느 쪽으로도 배지를 움직이지 않는다.
**판정된 적 없는 전제가 있으면 배지 상태를 만들어내지 않고 배지 자리를 비운다** — 판정 불가는 네 번째 배지 상태가 아니라 배지를 **그릴 수 없는 조건**이다. 깨진 전제가 하나라도 있으면 나머지를 판정하지 못해도 **달라짐**은 이미 확정이며, 직접 확인 전제뿐인 종목은 애초에 판정이 시세에 달려 있지 않다. 코드에서는 `badgeDisplay`.
자리를 비우는 이유는 **둘**이고, 화면은 둘을 갈라 말한다. **시세 축**: 자동 확인 전제의 판정은 시세에 달려 있으므로 시세가 조회 중이거나 조회 실패면 "유지 중"은 근거 없는 확신이 된다(#79, #81) — 화면은 시세를 원인으로 지목한다. **읽을 수 없음 축**: 시세가 정상으로 도착해도 전제가 **읽을 수 없음**이면 그 전제는 한 번도 판정된 적이 없다(#102) — 이 종목은 현재가가 멀쩡히 떠 있으므로 시세를 원인으로 지목하는 문구는 거짓이 되고, "시스템이 확인할 수 없는 근거가 있어요" 쪽으로 말한다. 두 축이 겹치면 **시세 축이 이긴다**: 시세는 재시도로 풀릴 수 있고 풀린 뒤에 읽을 수 없음 축이 남는다. 뒤집으면 재시도로 풀릴 일을 영구 판정 불가처럼 말하게 된다.
_Avoid_: 투자근거 유지/투자근거 변동(개념어 반복), 정상/이상

**시세 상태**:
화면이 보는 시세의 세 가지 상태. **조회 중** / **조회 실패** / **조회 완료**. "조회 중"과 "조회 실패"는 다른 것이며, 시도가 끝나기 전에 실패를 알리지 않는다 — 조회 중에는 가격도 전제 판정도 확정되지 않았으므로 배지·전제 상태를 함께 가린다.
"조회 실패"는 **종목 단위**다. KIS 조회 실패(5xx·레이트리밋·토큰 발급 실패)는 `getKoreanStockPrices`의 `Promise.allSettled`에 격리되어 그 종목의 시세만 `null`로 접히며, 서버 응답 자체를 실패시키지 않는다 — 시세가 안 나올 때 앱의 HTTP 오류를 찾아 나서면 방향이 틀린다 (#63).
_Avoid_: 로딩 실패(둘을 뭉갠다), 시세 없음(어느 쪽인지 말해주지 않는다)

**목록 상태**:
관심종목 목록을 화면이 아는지 여부. **확인 중** / **확인 완료** 두 가지뿐이며, **빈 목록은 확인 완료에서만 말한다** — 아직 모르는 것과 비어 있는 것은 다르다. 시세 상태가 "조회 중"과 "조회 실패"를 가르는 것과 같은 규율이며, 이유도 같다: 결판나지 않은 것을 결판난 것처럼 말하지 않는다.
하이드레이션 전은 **확인 중**이다. 이 구분이 없어서 홈 첫 프레임이 "관심 가는 종목을 담아보세요"를 먼저 보여준 적이 있다(#94).
**확인 완료에 이르는 길은 조회만이 아니다** — 담기·제외 액션이 성공으로 돌아오며 알려준 행도 확인 완료다. 서버가 이미 결판낸 것을 다시 물어야만 안다고 여기면, 돌아온 화면이 옛 목록을 그리는 동안 왕복을 기다리게 된다(#107). 낙관적 주장과 갈리는 지점은 **응답이 왔는가**다: 보내기 전에 미리 그리는 것은 여전히 확인 중이고, 그래서 `건너뛰기`도 `제외`도 응답을 기다렸다가 이동한다.
_Avoid_: 로딩(어느 쪽으로도 결판나지 않았다는 사실을 말해주지 않는다), 목록 없음

**변동 요약**:
**달라짐** 종목 수를 한 번에 알리는 장치. 표현은 **토스트**이며, `달라짐` 수가 0에서 N으로 올라가는 순간에만 지나간다.
**토스트를 독점하지는 않는다** — 매수·매도가 아직 구현되지 않았다는 안내도 같은 장치를 쓴다. 대신 **색이 둘을 가른다**: 변동 요약만 붉은 톤(`달라짐` 배지와 눈이 연결되도록)이고 나머지는 무채색이다. 한 색으로 두면 "달라진 게 있어요"와 "지원하지 않아요"가 같은 빨간 알약으로 번갈아 떠서 둘 다 흐려진다.
**진입점 역할은 지지 않는다** — 탭할 수 없고, 상세로 가는 길은 행의 배지가 맡는다. 요약이 하는 일은 주의 환기이지 이동이 아니다.
이름을 "배너"로 두면 표현 형태에 묶여, 표현이 바뀔 때마다 어휘가 함께 낡는다.
_Avoid_: 변동 배너, 알림 배너, 변동 요약 배너

**데모 시점**:
전제 판정과 시세를 어느 시점 기준으로 볼지 고르는 값. **현재** / **3개월 후** 두 개뿐. 화면 필터가 아니라 판정의 입력이다 — 시점이 바뀌면 배지 상태도 함께 바뀐다.
**머무는 화면은 `3개월 후`일 때 그 사실을 스스로 선언한다** — 값이 `localStorage`에 남아 링크나 새로고침으로 들어온 화면도 미래 기준 배지를 띄우는데, 조작 장치는 S1에만 있어 선언이 없으면 어느 시점을 보는지 알 길이 없다(#134). 시트·모달처럼 **스스로 임시인 표면은 선언하지 않고 뒤에 선 화면에 맡긴다.** 기준은 표면의 성격이지 진입 방식이 아니다 — "URL로 직접 열리는가"로 쓰면 S4의 전체 페이지 변종이 도로 걸린다. 선언은 앱의 상태(`3개월 후 보기가 켜져 있어요`)를 말하지 화면의 값이 가정값이라고 말하지 않는다: 시점이 바꾸는 것은 시드 종목의 시세뿐이다.
_Avoid_: 미래 보기, 시뮬레이션 모드, isFuture(불리언으로 다루면 판정 입력이라는 사실이 가려진다)

**자동 확인 / 직접 확인 필요**:
전제가 시스템에 의해 자동 판정되는지, 사용자가 스스로 확인해야 하는지를 가리키는 배지 쌍. 항상 두 배지가 함께 등장할 수 있음을 화면에서 감추지 않는다(설계 원칙).
**판정이 시스템 손을 떠난 전제는 자동 확인을 주장하지 않는다** — 배지와 그 아래 문구는 한 자리에서 함께 결정되며, 둘을 따로 계산하면 "자동 확인"과 "아직 직접 확인이 필요해요"가 한 줄에 공존한다(#88).
_Avoid_: quantitative/qualitative(사용자 대상 문구에서 — 코드 내부 타입명으로는 사용)

**읽을 수 없음**:
자동 확인 전제인데 판정이 **영구히** 불가능한 상태. 코드에서는 `unreadable`.
**시세를 기다리는 중과 다르다** — 앞은 기다리면 저절로 풀리고, 이쪽은 그렇지 않다. 한 값에 담으면 화면이 영구 판정 불가에도 "잠시 기다리는 중"인 것처럼 안내하게 된다.
**달라짐에는 투표하지 않지만, 배지를 그릴 수 있는지에는 관여한다.** `broken`이 아니므로 도달 목표와 같은 방식으로 배지 상태를 움직이지 못한다 — 그러나 판정된 적 없는 전제를 남기므로 배지 자리를 비운다(#102). 이 둘은 다른 말이다: 예전에는 "배지에 투표하지 않는다"가 "유지 중을 만든다"로 귀결되어, 시스템이 한 번도 판정하지 않은 종목을 화면이 "전제가 모두 유효하다"고 주장했다.
**둘을 가르는 기준은 원인이 아니라 회복 가능성이다.** 전제 쪽이 비어(기준·방향을 못 읽음) 판정이 막힌 경우뿐 아니라, **시세 쪽이 영구히 비는 경우도 이쪽이다** — 적자기업·신규상장처럼 KIS가 PER·PBR을 끝내 주지 않는 종목에 저평가선 전제가 걸리면 기다려도 값이 오지 않는다. 이 구분은 상장 종목 전체가 검색 대상이 되면서 실질이 생겼다: 데모 화이트리스트는 전부 대형주라 PER·PBR이 항상 나왔다.
**두 갈래는 회복 경로마저 다르다.** 설정을 못 읽는 쪽은 사용자가 근거를 다시 써야만 풀린다. 지표가 없는 쪽은 근거를 다시 써도 그대로지만, **실적이 바뀌어 그 지표가 나오기 시작하면 아무도 손대지 않아도 자동 확인으로 돌아온다** — 판정은 저장되지 않고 조회할 때마다 다시 이뤄지기 때문이다(ADR-0004). 여기서 **영구히**는 *기다림으로는 안 풀린다*는 뜻이지 *영원히 안 풀린다*는 뜻이 아니다. 이 구분이 없으면 "어차피 영영 판정 못 할 전제이니 저장할 때 직접 확인으로 내려두자"는 결론이 자연스러워 보이는데, 그것은 이 자동 복귀의 길을 닫는다(#111).
_Avoid_: 판정 불가(시세 없음까지 함께 가리켜 둘을 도로 뭉갠다), 오류

**상장 종목**:
KIS 종목 마스터에서 오는 KOSPI/KOSDAQ 주권 전체. 검색(S1.5)이 닿는 범위이며, 코드에서는 `LISTED_STOCKS`.
**업종을 모를 수 있다** — 마스터가 주는 것은 종목코드와 이름뿐이라, 상장 종목의 업종은 없는 것이 정상이다. 모르는 업종을 지어내지 않고 화면에서도 프롬프트에서도 그 자리를 비운다.
_Avoid_: 전 종목(데모 화이트리스트까지 포함하는 말처럼 들린다), 종목 마스터(출처의 이름이지 개념의 이름이 아니다)

**데모 화이트리스트**:
손으로 고른 27종. 시드 종목·인기 종목·큐레이션된 업종 라벨이 사는 곳이며, 코드에서는 `DEMO_WHITELIST`.
**검색 범위가 아니다** — 검색은 상장 종목 전체를 본다. 한 상수가 "앱이 아는 종목"과 "데모용으로 고른 종목" 두 역할을 겸하면, 인기 종목을 뽑는 데 상장 종목 전체를 훑는 코드가 아무 저항 없이 통과한다.
_Avoid_: 종목 유니버스/`STOCK_UNIVERSE`(두 역할을 겸하던 옛 이름), 데모 종목

**시드 보정**:
시드 종목의 전제 임계값·문구·미래 시세를 **그 시점의 실측 시세에서 계산해** 굽는 일. 세션 최초 프로비저닝 때 한 번 하고, 그 뒤로는 낡았을 때 조회 경로에서 다시 한다(ADR-0012).
**임계값은 절대값이 아니라 실측의 배수다.** 절대값 × 라이브 시세라는 조합이 #65·#152·#151로 세 번 같은 추격을 만들었다 — 시세가 임계값을 넘을 때마다 사람이 상수를 올리는 일이다. 배수로 두면 시세가 어디에 있든 세션이 시작되는 순간의 배지 상태가 언제나 의도한 것이다.
**보정 대상은 제품이 지어낸 픽션뿐이다** — `version = 1`인 thesis의 전제만 다시 굽는다. 심사자가 시드 종목에서 근거를 갱신하면 `is_seed`는 그대로 `true`인 채 최신 근거만 그 사람의 것이 되므로, `is_seed`로 거르면 방금 쓴 근거를 시스템이 덮어쓴다.
**기준값을 쓰는 것이지 판정을 쓰는 것이 아니다** — ADR-0004가 저장하지 않기로 한 것은 `status`/`observed_value`이고, 보정이 쓰는 것은 `statement`/`check_config`다. 조회하면서 쓴다는 이유로 둘을 같은 것으로 읽으면 이 장치가 통째로 지워진다.
_Avoid_: 시드 갱신(사용자의 `근거 갱신`과 헷갈린다), 리캘리브레이션, 시드 초기화(한 번뿐인 일처럼 들린다)

**섹션 카드**:
화면 본문의 한 절을 담는 카드. **카드를 쓴다면 한 섹션에 하나이고, 그 안의 항목은 카드가 아니라 행이다.**
`--card`와 `--background`가 둘 다 순백이라 카드를 카드로 만드는 것은 `ring` 하나뿐인데, 그 링이 항목마다 반복되면
한 화면 안에서 그 절만 잘게 분절돼 보인다(#137, #136).
**모든 섹션이 카드인 것은 아니다 — 카드는 아껴 쓸수록 말을 한다.** 카드가 화면에 하나뿐이면 그 하나가 무엇을
가리키는지가 뜻이 된다(S3에서는 "담은 뒤에 남는 것"이 전제뿐이라 전제 섹션만 카드다). 섹션마다 카드를 두르면
그 뜻이 사라지고, 카드 사이에 놓인 라벨은 어느 쪽에도 속하지 않는 것처럼 보인다. 카드가 없는 섹션의 항목은
불릿 리스트로 세우며, 그때 무게를 주는 것은 카드가 아니라 **회색을 걷는 일**이다.
**행을 가르는 것은 간격뿐이다 — 구분선을 긋지 않는다.** 선은 이 앱에서 이미 뜻을 갖고 있어서(근거 카드의 실선은
문단 구분, 가격 흐름의 점선은 "위는 관측값 아래는 파생값") 항목 사이에 하나 더 그으면 방금 걷어낸 분절이 선의
형태로 되살아난다. 대신 **행 안 간격과 행 사이 간격의 비**가 유일한 경계가 되므로, 둘 중 하나만 손대지 않는다.
같은 이유로 특정 행에 배경 틴트를 깔지 않는다 — 그 행만 카드처럼 읽힌다. 깨진 전제의 강조는 붉은 문구가 맡는다.
**빈 상태는 이 카드가 아니다.** 항목이 없을 때의 `bg-muted` 블록은 여러 섹션이 공유하는 별개 문법이라, 카드로
바꾸면 섹션마다 빈 상태가 갈린다.
_Avoid_: 항목 카드/리스트 카드(항목마다 카드를 두는 폐기된 문법의 이름이다), 섹션 박스

**뷰포트 액자**:
넓은 화면에서 모바일 뷰포트를 감싸 어디까지가 서비스인지 알려주는 테두리. 코드에서는 `PhoneFrame`.
**기기 목업이 아니다** — 아이폰을 흉내내지 않으며 베젤도 노치도 없다. 액자가 하는 일은 경계를 긋는 것이지 기기를 연기하는 것이 아니다. 따라서 액자의 생김새는 기기에서 따오지 않고 **앱의 디자인 언어를 따른다** — 모서리 곡률도 예외가 아니어서, 액자는 그 안의 카드보다 오히려 덜 둥글다(#125). 목업으로 읽으면 이게 어색해 보여 기기다운 값으로 되돌리고 싶어지는데, 그러면 곡률이 헤더를 파고든다.
**실제 기기에서는 없는 것이다.** 좁은 화면에서 뷰포트는 화면 전체를 채우고 액자는 사라진다 — 액자에 손대는 변경은 데스크톱에만 닿는다.
_Avoid_: 폰 프레임/디바이스 프레임(기기 목업으로 읽혀 위 결정을 되돌리게 만든다), 목업

**화면 헤더**:
모든 화면 맨 위에 얹히는 띠. 코드에서는 `ScreenHeaderShell`이 외곽을, `ScreenHeaderRow`가 **타이틀 행**을 맡고, 뒤로가기가 있는 화면은 그 위에 얹은 `ScreenHeader`를 쓴다.
**타이틀 행 높이는 어느 화면에서나 같다.** 그 높이는 뒤로가기 버튼이 차지하는 자리에서 왔고, **버튼이 없는 화면에서도 비워둔다** — 홈(S1)처럼 뒤로갈 곳이 없어도 마찬가지다. 자리를 안 잡으면 행이 타이틀 줄높이로 주저앉아 그 화면만 헤더가 낮아지고, 화면을 넘나들 때 타이틀 베이스라인이 뛴다(#129).
**세로는 공유하지만 가로는 아니다.** 뒤로가기가 있는 화면의 타이틀은 버튼만큼 들여쓰이고, 홈은 들여쓰지 않는다 — 영영 버튼이 안 들어올 자리를 비워두면 좌상단에 이유 없는 여백이 생긴다.
**타이틀 행 아래에 무엇이 얼마나 쌓이는지는 화면의 몫이다.** 헤더의 총 높이는 애초에 고정값이 아니다 — 홈은 조건부 상태 문장이 쌓이고 다른 화면은 보조문구가 붙는다. 그래서 행 사이 간격은 셸이 소유하지 않는다. 여기를 헤더가 소유하면 "달라붙어야 하는 캡션"과 "각자 서야 하는 상태 문장"이 한 값을 두고 다투고, 셸이 화면 목록을 알게 된다.
_Avoid_: 헤더 높이(총 높이로 읽혀 조건부 행까지 맞추려 들게 만든다), 헤더 프레임(뷰포트 액자와 충돌한다), 툴바/앱바
