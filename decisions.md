# decisions.md

「사기 전에」(저장소 `before-buy`) 프로토타입을 만들면서 내린 판단을 기록한 문서입니다.

2026년 8월 25일부터 28일까지 만들면서 그때그때 적었고, 2026년 9월 23일에 비어 있던 항목을 채워 저장소에 올렸습니다. 나중에 채운 부분은 **기록 보완**으로 표시했습니다. 코드와 관련된 결정은 `docs/adr/`에 따로 있고, 여기에는 왜 그렇게 정했는지를 적었습니다.

## 먼저 읽을 것

만들면서 내린 판단 중 제품 방향을 정한 것은 세 가지입니다. 셋 다 AI가 먼저 제안한 것이 아니라 제가 문제를 발견하고 방향을 정했습니다. 근거는 아래 항목에 있습니다.

| | 판단 | 어디에 |
|---|---|---|
| **1** | 묻는 시점을 매수 직전이 아니라 관심종목에 담을 때로 정했습니다. 매수 직전에 입력을 받으면 그 사이 가격이 움직이고, 이미 사기로 마음먹은 사람에게 반박을 보여줘도 잘 받아들여지지 않습니다. 담을 때 묻고, 살 때는 적어둔 것을 보여주기만 합니다. | [B-2](#b-2-개입-지점을-매수-직전에서-관심종목-등록으로-이동) |
| **2** | AI 응답을 세어 보고 호출 방식을 바꿨습니다. 처음 방식에서는 한국어 문장의 41.8%가 깨졌고, 출력 방식 3가지와 모델 2가지를 비교해 구조화 출력으로 바꾸니 0%가 됐습니다. 모델을 올리지 않아도 돼서 비용은 그대로였습니다. | [E-1](#e-1-강제-tool-call을-버리고-구조화-출력으로-58-adr-0003), [ADR-0003](docs/adr/0003-structured-output-over-forced-tool-call.md) |
| **3** | 규제와 권한 문제를 설계 단계에서 처리했습니다. 프롬프트에 "종목 평가는 하지 않고 근거 검증만 한다"를 예문으로 넣었고, 매도를 권하는 것처럼 보이는 버튼은 화면에서 뺐고, 조회용 키와 거래용 키를 파일 단위로 분리했습니다. | [A-4](#a-4-규제를-리스크가-아니라-설계-제약으로-다루기), [C-9](#c-9-프롬프트-원칙을-다시-씀--내부-정합성이-핵심-기능을-억제할-뻔함), [B-8](#b-8-규제-원칙을-화면-액션-레벨까지-관철), [C-5](#c-5-한투-api-도메인-분리--조회는-실전-거래는-모의) |

**돌아보며 (2026년 9월)** — 이 세 판단은 만들 당시에 이미 기록해 두었지만, 제출한 문서에는 넣지 못했습니다. 필수 항목을 채우는 데 집중하다 보니 핵심 판단은 별도 항목으로 생각해 빼놓았고, 비즈니스 목표도 거래나 자산이 아니라 "관심종목 체류·재방문"으로 썼습니다. 그래서 저장소를 다시 열면서 README 첫 화면에 비즈니스 목표와 지표를 올리고, 이 표를 문서 맨 위에 두었습니다. 만든 것에 비해 전달이 약했다는 것이 이 프로젝트에서 배운 점입니다.

## 이 문서를 쓰는 방식

각 항목에 무엇을 AI에 맡겼고, 무엇을 제가 판단했고, AI 제안 중 무엇을 뒤집었는지를 함께 적었습니다.

AI를 많이 쓴 작업일수록 결과물이 누구 실력인지 흐려지기 쉽습니다. 그래서 결과보다 판단의 소재를 남기려 했고, 특히 AI 제안을 받아들이지 않은 기록을 빠뜨리지 않으려 했습니다.

표기 규칙

- 🧑 제가 판단한 것
- 🤖 AI가 제안했고 받아들인 것
- ↩️ AI가 제안했지만 뒤집거나 수정한 것

---

## A. 문제 정의와 리서치

### A-1. 리서치를 '확인'이 아니라 '반증'으로 설계

**상황** — "투자 흐름에서 결정 단계가 비어 있다"는 가설을 세운 뒤, 시장 조사를 어떻게 할지 정해야 했습니다.

**결정** — 가설을 뒷받침하는 사례를 모으는 대신, 가설을 깨는 사례를 먼저 찾도록 리서치 프롬프트를 구성했습니다. "결정 단계를 이미 점유한 서비스가 있으면 즉시 알려달라"를 명시하고, "내 가설에 맞춰주려 하지 말라"는 지시를 넣었습니다.

**근거** — 이미 같은 제품이 있다면 이틀을 만들고 나서가 아니라 첫날 오전에 알아야 했습니다. 확인용으로 조사하면 원하는 답이 나오기 쉽습니다.

🧑 반증 우선 방침, 프롬프트에 넣을 제약 조건
🤖 검색 실행과 1차 분류

**기록 보완** — 2026년 8월 25일(Day 1) 오전에 반나절 정도 걸렸습니다. 검색은 Perplexity, 분류는 Claude로 했습니다. 국내 증권사 AI 기능, 해외 리테일 AI 투자 서비스, 개인이 만든 도구의 세 부류로 나눠 찾았고, 검색어는 부류별로 "카카오페이증권/토스증권/미래에셋/키움 AI 기능", "Danelfin/Composer/robo-advisor AI investment", "GitHub investment thesis tracker", "devil's advocate AI investment thesis", "노션 매매일지 템플릿", "premortem 매수 전 체크리스트" 등을 썼습니다. 개인 도구는 GitHub, Reddit, App Store, Notion 마켓플레이스를 중심으로 봤습니다. 수집 표와 단계별 정리는 `docs/research/투자 흐름 4단계 AI 제품 매핑  결정 단계 공백 가설 검증.md`에 있습니다. 1차 결과에 "가설이 부분적으로 깨졌다"는 판정이 들어 있었고, 그 처리가 A-2입니다.

---

### A-2. 1차 리서치 결과 중 3건을 오분류로 정정

**상황** — 리서치 결과 "가설이 부분적으로 깨졌다"는 보고를 받았습니다. 반증 사례로 제시된 항목들을 직접 열어 확인했습니다.

**확인 결과**

| 항목 | 1차 분류 | 실제 | 근거 |
|---|---|---|---|
| unicodeveloper/devilsadvocate | 개인 자작 도구 | 기관용(펀드매니저) | README가 "for fund managers", House View·IC 메모 워크플로 기반. Star 8 |
| AskMADE / tellodb / suprmind | 리테일 투자 서비스 | 범용 토론 툴 | 유즈케이스 12개 중 투자는 1개. 계좌 연동·시점 개입·추적 없음 |
| Index Guard | "감정 냉각만 유도" | 과소평가 | 상황별 질문 기능이 있음. 9개월간 평가 1건이라는 점이 더 중요했음 |

**결정** — 반증 목록을 다시 분류했고, 리테일 상용 제품은 여전히 0건으로 확인됐습니다. 가설은 유지하되 문장을 정확하게 고쳤습니다.

> ~~결정 단계가 비어 있다~~
> → "왜 사는가"를 다루는 제품은 기관용과 개인 자작 영역에만 있고, 리테일 금융사 중 제품화한 곳은 없다

↩️ AI의 "가설이 깨졌다"는 판정을 뒤집었습니다. 뒤집은 근거는 직관이 아니라 원문 확인이었습니다.
🧑 실물 확인 대상 선정, 재분류 판단

---

### A-3. 국내 유사 시도 12종 발견 → 문제 정의 자체를 교체

**상황** — Index Guard와 InvestNote의 앱스토어 연관 앱 목록에서, 국내에 매매일지·투자원칙 기록 앱이 12종 이상 있다는 것을 확인했습니다. 1차 리서치가 놓친 카테고리였습니다.

**결정** — 문제 정의를 바꿨습니다.

> ~~개인 투자자는 매수 시점에 자기 근거를 언어화하지 않는다~~
> → 언어화하려는 시도는 이미 많다. 문제는 기록이 거래와 분리되어 있다는 것이다

**근거** — 12종이 있다는 것은 수요가 있다는 뜻이면서 실패 사례이기도 합니다. 공통 원인은 세 가지로 정리됐습니다. 계좌 밖에 있어 손으로 입력해야 하고, 주문 흐름 밖에 있어 매수 시점에 열리지 않고, 기록만 하고 검증하지 않습니다.

**부수 효과** — 개인 경험(Invest Diary 1년)이 한 사람의 일화가 아니라 구조적인 실패의 한 사례가 됐습니다. 제안의 약점 하나가 없어졌습니다.

🧑 앱스토어 연관 앱 목록을 단서로 읽은 것, 문제 정의 교체
🤖 12종의 공통 한계 정리

---

### A-4. 규제를 '리스크'가 아니라 '설계 제약'으로 다루기

**상황** — AI가 "이 종목을 사라"고 말하면 자본시장법상 투자자문업 영역에 들어갑니다.

**결정** — 피해야 할 위험이 아니라 제품 구조를 정하는 제약으로 삼았습니다. 추천하지 않고 사용자가 이미 가진 근거를 검토하는 설계는, 규제를 피하려고 타협한 결과가 아니라 이 제약에서 나온 형태입니다.

**검증** — Index Guard와 InvestNote가 서로 무관한 개발사인데도 거의 같은 면책 문구를 쓰고 앱스토어 심사를 통과해 운영 중인 것을 확인했습니다. 통용되는 방식입니다.

🧑 규제를 설계 제약으로 올린 판단
🤖 자본시장법 경계에 대한 초기 지적

---

## B. 제품 설계

### B-1. 네 가지 컨셉 중 '근거 검증'을 선택

**검토한 대안**

| 안 | 내용 | 탈락 사유 |
|---|---|---|
| 자연어 전략 → 백테스트 | 조건 변환·검증·알림 | LLM 없이도 됨. 지원자 대다수가 고를 안 |
| 매매 편향 진단 | 거래내역 기반 행동 편향 분석 | 사후 분석에 가까워 '결정'이 아님 |
| 나만의 투자 툴 빌더 | 자연어로 대시보드 생성 | 4일 안에 얕아 보일 위험 |
| **근거 검증** | 근거를 묻고 검토하고 추적 | **채택** |

**근거** — 과제 문구의 핵심어가 "더 나은 투자 결정"입니다. 나머지 셋은 실행이나 회고를 다룹니다. 그리고 이 안만 LLM 없이는 만들 수 없습니다. 자유 서술을 읽고 반증을 구성하고 관측 가능한 지표로 옮기는 일은 다른 기술로 대체하기 어렵습니다.

🧑 최종 선택
🤖 대안 제시와 각 안의 장단점 정리

---

### B-2. 개입 지점을 매수 직전에서 관심종목 등록으로 이동

이 프로젝트에서 가장 크게 바뀐 판단입니다.

**초기 설계** — 매수 주문 직전에 "왜 사는가"를 묻는 구조. 논리적으로는 결정의 순간에 가장 가깝습니다.

**문제 발견** — 실제 서비스에 올린다고 가정하니 두 가지가 걸렸습니다.

1. 시장이 움직이는 중에 입력을 요구하면 체결 가격이 밀립니다. 사용자에게는 불편이 아니라 금전적인 손해입니다.
2. 매수 버튼을 누른 시점에는 이미 결정이 굳어 있습니다. 그때 반박을 보여주면 다시 생각하기보다 방어적으로 반응하기 쉽습니다. UX만 나쁜 게 아니라 효과도 없습니다.

**결정** — 진입점을 관심종목 등록으로 옮기고, 매수 시점에는 새로 묻지 않고 이미 쓴 근거를 보여주기만 합니다.

```
관심종목 등록 → 근거 입력 → AI 검토 → 전제 저장
                                    ↓
매수 주문 시 → 저장된 근거를 보여줌 (묻지 않음)
```

**부수 효과** — 세 가지가 같이 해결됐습니다. 기존 기능에 얹는 구조가 되어 실현 가능성이 올라갔고, 주문 흐름을 건드리지 않아 거래량과 부딪히지 않게 됐으며, 근거를 안 쓴 사용자에게는 마찰이 없어졌습니다.

🧑 UX 관점의 문제 제기와 관심종목 대안 제시
↩️ 초기 설계(매수 직전 개입)를 폐기. 이 판단은 AI가 먼저 내놓지 않았습니다
🤖 "결정이 굳은 뒤라 효과도 낮다"는 심리적 근거 보강, 부수 효과 정리

---

### B-3. 입력을 객관식 우선으로, 다만 자유 서술은 유지

**문제** — "왜 사려고 하세요?"라는 빈 텍스트 박스를 주면 대부분 "오를 것 같아서"라고 씁니다. 근거가 아니라 감상이라 검증할 재료가 되지 않습니다. 모바일에서 긴 글을 쓰기 어렵다는 문제도 있습니다.

**결정** — 카테고리 선택 → AI 후속 질문 → 짧은 자유 서술(선택) 구조.

**설계 포인트** — 선택지에 "누가 추천해서", "그냥 느낌"을 그대로 넣었습니다. 고르는 순간 사용자가 스스로 알아차리기 때문에, AI가 말하기 전에 절반은 효과가 납니다.

**뒤집은 것** — 완전 객관식은 하지 않았습니다. 그러면 앞서 본 12종의 '체크리스트 아니오 개수 세기'와 다를 게 없어집니다. 자유 서술을 AI가 읽고 반박한다는 것이 이 제품의 차별점이라 없앨 수 없었습니다. 긴 글 한 번 대신 짧은 답 여러 번으로 균형을 잡았습니다.

**부수 효과** — 카테고리가 추적 로직에 직접 매핑되어 전제 추출 구현이 단순해졌습니다.

🧑 모바일 입력 문제 제기, 객관식 우선 방향
🤖 "입력 편의가 아니라 입력 품질이 문제"라는 재해석
↩️ 객관식만 남기자는 방향을 되돌림

---

### B-4. 무조건 반박하지 않는다

**결정** — AI가 반박할 만한 점이 약하면 억지로 반박하지 않습니다. LLM 출력 스키마에 `is_challengeable` 필드를 두고, `false`이면 검토 UI를 그리지 않습니다.

**근거** — 매번 반박당하면 사용자가 지칩니다. 근거 없는 반박은 제품 신뢰를 깎습니다.

🧑 조건부 검토 원칙
🤖 이를 구현으로 옮기는 방법(스키마 필드 + 렌더 분기)

---

### B-5. 용어를 세 층위로 분리

**문제** — 문서 전반에서 "논지"라는 단어를 쓰고 있었는데 어색했습니다.

**진단** — 논지(論旨)는 논문이나 평론의 말이고, investment thesis의 직역이라 기관 투자자의 어휘입니다. 부록에서 기관용 도구들과 선을 그어놓고 그쪽 단어를 쓰면 포지션이 흐려집니다.

**결정** — 하나로 통일하지 않고 층을 나눴습니다.

| 층위 | 어휘 |
|---|---|
| 코드·DB | `thesis`, `premise` (영어 식별자) |
| 문서 | 투자 근거 / 전제 |
| UI | 개념어 없이 질문형 — "왜 이 종목이 궁금하세요?" |

LLM 시스템 프롬프트에도 "논지·가설 같은 개념어 대신 일상어를 쓴다"를 넣었습니다. 이게 없으면 AI가 반박문에서 개념어를 써서 UI 문구를 다듬어도 소용이 없습니다.

🧑 어색함 감지
🤖 원인 진단(기관 어휘라는 점)과 층위 분리 제안

---

### B-6. 화면을 그려보고 나서 스펙의 오류 세 개를 발견

**상황** — 구현 전에 와이어프레임을 그렸습니다(D-1). 텍스트 명세로는 문제없어 보이던 것들이 화면에서 드러났습니다.

**발견 1 — 종목 검색 화면이 없었다**

기술 스펙에 S1(목록)과 S2(입력)만 있고, 종목을 어떻게 찾아서 담는지가 빠져 있었습니다. 검색 없이는 코어 루프가 시작되지 않는데도 명세에 없었습니다. S1.5로 신설했습니다.

**발견 2 — 관심종목과 보유종목이 한 목록에 섞여 있었다**

초안에서는 모의투자 계좌의 보유 종목을 그대로 목록에 노출하려 했습니다. 그런데 관심종목은 "사고 싶은데 아직 아니다"이고 보유는 "이미 샀다"라서, 근거를 쓰는 시점과 보여주는 시점이 다릅니다. 섞으면 제품 논리가 흐려지고, 보유 종목에 '구매' 버튼이 뜨는 모순도 생깁니다.

두 섹션으로 나눴습니다. 데이터 모델의 `status`가 이미 그 구분을 갖고 있었는데 화면에서만 뭉개져 있었습니다.

**발견 3 — 배지가 2상태로는 부족했다**

`유지` / `변동` 두 가지만 있었는데, 근거를 쓰지 않고 담은 종목이 갈 곳이 없었습니다. `근거 없음`을 추가해 3상태로 만들었습니다.

🧑 화면을 먼저 그리기로 한 결정, S1.5 신설
🤖 발견 2·3의 지적
↩️ 초기 명세의 화면 구성을 폐기하고 다시 작성

> D-1에서 디자인을 먼저 하기로 한 이유가 여기서 확인됐습니다. 이 세 가지는 텍스트 명세만 읽어서는 보이지 않았습니다.

---

### B-7. 화면 편의보다 측정 가능성을 우선

**상황** — S4(주문 전 확인)를 bottom sheet로 줄이면서 수량 입력이 빠졌습니다. 취소/구매 두 버튼만 남아 화면은 가벼워졌습니다.

**문제** — 핵심 지표가 "근거를 본 뒤 수량을 조정했는가"인데, 수량을 입력하는 자리가 없으면 `initial_qty` / `final_qty`를 채울 수 없습니다. 가설 2의 검증이 반쪽이 됩니다.

그리고 근거를 보고 생각이 바뀐 사용자가 갈 곳도 없었습니다. 취소하면 근거는 옛날 상태로 남습니다. 가설 2가 정의한 세 가지 행동 변화(수량 조정 / 취소 / 근거 갱신) 중 두 개를 측정할 수 없는 상태였습니다.

**결정**

- 수량 입력을 bottom sheet 안에 넣습니다. 근거를 보면서 얼마나 살지 정하는 흐름이 자연스럽고 화면도 하나 줄어듭니다
- `다시 생각하기`는 버튼으로 추가하지 않고 조건부로 노출합니다. 전제가 하나 이상 깨진 상태일 때만 근거 카드 아래에 링크가 뜹니다

**근거** — 필요한 순간에만 나타나고 평소에는 방해하지 않습니다. 버튼을 셋으로 늘리면 시트가 무거워집니다.

🤖 지표 손실 지적과 조건부 노출 설계
🧑 채택 판단

> PRD에서 지표를 "가설이 틀렸을 때 틀렸다고 판정할 수 있는 형태"로 잡아둔 것이 화면 설계를 되돌리는 근거가 됐습니다. 지표가 먼저 정의되어 있지 않았다면 이 문제를 발견하지 못했을 것입니다.

---

### B-8. 규제 원칙을 화면 액션 레벨까지 관철

**상황** — S5(관심종목 상세)에 `매도 검토` 버튼을 두려 했습니다.

**결정** — 뺐습니다. `생각 업데이트 하기`와 `관심종목에서 제외`만 남깁니다. 보유 종목에서는 제외 액션도 없앴습니다.

**근거** — 제품이 매도를 제안하는 형태가 되면 설계 원칙(추천하지 않는다)과 규제 경계 양쪽에 걸립니다. 정보를 보여주고 판단은 사용자에게 남깁니다.

원칙을 문서에만 적어두면 화면에서 새어 나갈 수 있습니다. 버튼 하나로도 원칙이 무너집니다.

**이후** — 보유 종목에는 사용자 테스트 뒤 매수와 매도를 같은 폭, 같은 위계로 나란히 열었습니다(ADR-0009). 한 방향만 열면 그 자체가 권유가 되기 때문입니다. 원칙은 "매도를 제안하지 않는다"가 아니라 "방향을 고르지 않는다"였습니다.

---

### B-9. 제품명 「사기 전에」

**결정** — 저장소 `before-buy`, 제품명 「사기 전에」, 태그라인 "사기 전에 — 왜 담았는지 기억하기".

**검토한 대안** — `still-valid`, `why-watch`, `thesis-check`, 「투자아이디어 검증기」

**근거** — 제품이 하는 일이 그대로 이름이 됩니다. '담다'가 관심종목의 동사라서, 태그라인이 "매수가 아니라 담는 순간에 개입한다"를 설명 없이 전달합니다.

`thesis-tracker`나 `devils-advocate` 계열은 피했습니다. 부록 A에 정리한 기존 프로젝트들과 겹쳐서 검색하면 남의 것이 먼저 나옵니다.

**주의** — 붙여 쓰면 '사기(詐欺) 전에'로도 읽힙니다. 증권사 서비스라 로고와 앱 타이틀에서 띄어쓰기를 살립니다.

---

## C. 기술 결정

### C-1. 기존 프로젝트를 포크하지 않고 새 저장소 + 어댑터만 이식

**상황** — 개인 프로젝트 Invest Diary에 한국투자증권 API 연동이 이미 구현되어 있었습니다. 통째로 포크하면 하루를 아낄 수 있었습니다.

**결정** — 새 저장소에서 시작하고 API 어댑터 계층만 옮겼습니다.

**근거** — 실질적인 이득은 인증·토큰 갱신·레이트리밋 대응 같은 어댑터 노하우에 있고, 그건 파일 몇 개면 옮겨집니다. 반면 전체를 가져오면 1년치 개인 프로젝트 커밋 위에 4일 작업이 얹혀 무엇이 이번 작업인지 흐려집니다. 저장소를 제출하는 이상 커밋 히스토리가 구현 과정의 증거입니다.

↩️ AI는 처음에 "포크하고 명시하라"를 권했습니다. 저장소 제출이 확정되면서 판단이 바뀌었습니다.

---

### C-2. 인증 없음 — 익명 세션

**결정** — 로그인을 만들지 않습니다. httpOnly 쿠키로 세션 ID를 발급하고 모든 레코드에 붙여 격리합니다.

**근거** — 심사자 진입 마찰이 없어야 합니다. 완전 공유 방식은 심사자끼리 서로의 입력을 보게 되고 데이터가 섞입니다. 세션 격리는 미들웨어 하나와 쿼리 필터 하나면 되므로 비용 대비 효과가 분명합니다.

**연결** — 포트폴리오에서 스스로 지적했던 권한 모델 미검토 문제를 이번에는 처음부터 잡고 갑니다.

---

### C-3. DB를 Supabase에서 Neon으로

**제약** — Supabase 무료 티어 프로젝트 한도(2개)가 이미 차 있었습니다.

**검토한 대안**

- 기존 프로젝트 일시정지 → 기각. 포트폴리오에 링크한 데모 두 곳(Invest Diary, 숲마루)이 멈춥니다. 심사 중에 심사자가 그 링크를 열어볼 수 있습니다
- Render 무료 Postgres → 생성 후 30일 만료, 14일 유예 후 데이터 삭제. 채용 절차가 몇 주 이어질 것을 감안하면 위험
- **Neon → 채택.** 만료 없음, Postgres 호환, Vercel 연동

**부수 효과** — 이 프로젝트는 Auth·Realtime·Storage를 쓰지 않아 호스팅 Postgres만 있으면 됐습니다. 처음부터 Supabase일 이유가 없었던 셈입니다.

🧑 제약 발견
🤖 대안 비교, 포트폴리오 데모가 멈춘다는 리스크 지적

---

### C-4. RLS를 버리고 서버 레이어 단일 진입점으로

**상황** — 초기 스펙에는 "Supabase RLS로 강제하거나 서버 액션에서 처리"라고 두 갈래를 남겨뒀습니다.

**결정** — RLS를 빼고 서버 레이어로 단일화했습니다.

**근거** — RLS는 인증 토큰과 물려 동작하는 구조인데 이 프로젝트는 익명 쿠키 세션입니다. 처음부터 맞지 않는 선택지였고, DB를 바꾸면서 이 애매함이 드러났습니다.

**적용 규칙**

- 세션 ID는 서버에서만 읽음 (httpOnly). 클라이언트가 지정 불가
- 모든 DB 접근은 `session_id`를 인자로 받는 헬퍼를 경유. 쿼리 직접 작성 금지
- 리소스 ID 조회 시에도 소유 세션 검증 (다른 세션의 UUID를 알아내도 조회 불가)

---

### C-5. 한투 API 도메인 분리 — 조회는 실전, 거래는 모의

**기록 보완** — 2026년 8월 25일(Day 1) 오후, Invest Diary에서 옮긴 어댑터(C-1)를 모의투자 도메인으로 바꿔 시세 조회를 확인한 직후, 재무비율 TR(`FHKST66430300`)을 같은 도메인으로 호출하자 데이터가 오지 않았습니다. 한국투자증권 개발자센터 문서로 다시 확인한 결과, TR_ID는 실전과 모의가 같지만 모의투자 계좌에는 재무비율 데이터가 제공되지 않습니다(`docs/research/kis-portal-verification.md`). 같은 날 `KIS_TR_ID_DEMO`에서 `financialRatio`를 빼는 커밋을 남기고 대안을 이슈 #9에서 정했습니다. 도메인을 나눈 뒤 실전 조회 키로 응답을 검증(#13)하다가 E-3의 발견으로 이어졌습니다.

**제약** — 모의투자 도메인에서는 재무비율을 얻을 수 없습니다. 밸류에이션 전제(PER/PBR)를 쓰려면 조회를 실전 도메인으로 보내야 합니다.

**검토한 대안**

- `valuation` 전제를 포기하고 `price`만 사용 → 가능하지만 '저평가' 카테고리의 전제를 자동 판정할 수 없게 됨
- DART 등 별도 재무 API 추가 → 공시 파싱 부담이 크고 PER은 직접 계산해야 함. 4일에는 과함
- **도메인 분리 → 채택.** 실전 앱키를 이미 갖고 있어 추가 비용이 거의 없었습니다

**결정** — 클라이언트를 파일 단위로 나눴습니다.

```
lib/kis/quote-client.ts   실전 도메인 · 조회 전용
lib/kis/trade-client.ts   모의 도메인 · 주문/잔고
```

같은 클래스에 플래그로 분기하지 않은 이유는, 거래 코드 경로가 실전 자격증명을 참조할 방법 자체를 없애기 위해서입니다. 환경변수 이름에도 용도를 넣었습니다(`KIS_REAL_APPKEY_READONLY`).

**부수 효과** — 제약 때문에 도메인을 나눴는데, 그 결과 읽기와 쓰기 권한이 물리적으로 분리됐습니다.

↩️ AI는 처음에 "valuation을 버리고 price로 가라"고 권했습니다. 실전 앱키가 있다는 정보가 들어오면서 판단이 바뀌었습니다.

---

### C-6. LLM 호출을 하나로 통합

**결정** — 근거 검토와 전제 추출을 별도 호출로 나누지 않고, structured output 한 번에 둘 다 받습니다.

**근거** — 두 번 부르면 사용자가 두 번 기다립니다. 체감 속도가 중요한 화면입니다.

---

### C-7. 구현 범위와 잘라낼 순서를 미리 정함

**결정** — 코어 루프 ①②③④는 실제로 동작하게, ⑤ 추적은 자동 판정이 가능한 전제만.

그리고 시간이 부족할 때 버릴 순서를 시작 전에 정해뒀습니다.

1. 데모 시나리오 모드 → 정적 화면 대체
2. `valuation` 판정 → `price`만
3. Cron 배치 → on-demand 판정
4. 실제 주문 → 주문 직전 화면까지만
5. AI 후속 질문 동적 생성 → 고정 질문

**지킬 것** — ①②③ 코어 루프, 세션 격리, 보안 체크리스트, 이 문서의 실시간 기록.

**근거** — 마감이 닥치면 즉흥적으로 자르게 되고, 그때 가장 지켜야 할 것을 자르기 쉽습니다. 순서를 미리 정해두면 판단이 빨라집니다.

**기록 보완** — 실제로 잘린 것은 3번(Cron, E-2에서 필요 없어짐)과 4번(실제 주문, E-6)이었고, 1·2·5번은 남았습니다.

---

### C-8. 자동 판정의 한계를 제품 안에 드러내기

**결정** — 전제를 저장할 때 AI가 판정 방식까지 함께 출력하게 했습니다. 자동 확인이 가능한 전제와 불가능한 전제를 생성 시점에 나눠, 화면에 `자동 확인` / `직접 확인 필요` 배지로 보여줍니다.

**근거** — "경쟁사가 진입하지 않는다" 같은 정성적 전제는 자동으로 판정할 수 없습니다. 이걸 숨기면 제품이 거짓말을 하게 되고, 드러내면 한계를 아는 설계로 읽힙니다. 문서로 따로 설명할 필요도 없어집니다.

---

### C-9. 프롬프트 원칙을 다시 씀 — "내부 정합성"이 핵심 기능을 억제할 뻔함

**상황** — LLM 시스템 프롬프트 초안(issue #42)에서 원칙 2를 이렇게 잡았습니다.

> "그 종목이 실제로 좋은지 나쁜지가 아니라, 사용자가 말한 이유들이 서로 앞뒤가 맞는지를 봅니다"

**문제** — 이 문장은 순수한 내부 정합성만 허용합니다. 그런데 같은 프롬프트의 원칙 4는 전제를 PER/PBR 같은 관측값으로 바꾸라고 하고, 입력에는 현재가·PER·PBR이 들어갑니다.

무엇보다 이 제품에서 가장 값진 출력이 외부 데이터 대조입니다. "동종업계 대비 싸다고 하셨는데 업종 평균은 더 낮습니다" 같은 지적은 내부 정합성이 아닙니다. 원칙 2를 그대로 따르면 모델이 이런 반박을 억제합니다.

**진단** — 경계는 "내부냐 외부냐"가 아니라 "사용자 근거를 검증하느냐, 종목을 평가하느냐"입니다. 그것이 규제 선이기도 합니다.

**결정** — 원칙 2를 다시 쓰고, 프롬프트 안에 두 문장을 대비시켜 넣었습니다.

```
"이 종목은 비쌉니다"            → 종목 평가. 하지 않는다
"동종업계 대비 싸다고 하셨는데,   → 근거 검증. 한다
 업종 평균 PER은 더 낮습니다"
```

**부수 결정**

- `statement`에 "자동 확인 불가" 같은 처리 방식을 넣지 않습니다. UI에 이미 배지가 있어 중복이고, `check_type` 필드가 그 정보를 담습니다. 데이터와 표현을 분리합니다
- `counterpoints` 2~3개, `premises` 2~4개로 상한을 두고, 가능하면 최소 하나는 자동 판정이 가능한 것으로 만들게 했습니다. 전부 `qualitative`면 추적할 게 없습니다
- 규칙을 늘리는 대신 출력 예시 두 벌을 넣었습니다. 원칙 1(추천 금지)과 5(일상어)는 부정 제약이라 규칙만으로는 자주 어깁니다

↩️ 초안의 원칙 2를 폐기하고 다시 작성
🤖 충돌 진단과 대체 문안

---

### C-10. 실제 지표를 넣으니 예시가 더 좋아짐

**상황** — few-shot 예시를 임의의 숫자로 만들어두었다가, 실제 시세로 바꿨습니다. (SK하이닉스 PER 7.53 / PBR 4.56, 2026.08, 네이버페이 증권)

**발견** — PER은 낮은데 PBR은 높은 조합이 나왔습니다. 보통 이익이 유난히 좋은 국면에서 나타나는 모양이고, 메모리 사이클에서는 실적 정점 근처에서 PER이 가장 낮아 보입니다.

이걸 예시 A의 첫 번째 반박으로 삼았습니다.

> "PER은 7.53배로 낮은데 PBR은 4.56배로 높습니다. 두 숫자가 다른 이야기를 하고 있어요."

**의미** — 종목을 평가하지 않으면서(비싸다/싸다를 말하지 않으면서) 사용자가 놓친 것을 드러냅니다. 규제 선을 넘지 않고 도움을 주는 것이 어떤 모양인지를 규칙 열 줄보다 이 한 문장이 더 잘 보여줍니다.

이 패턴을 카테고리 지침에도 반영했습니다. "PER과 PBR이 서로 다른 이야기를 하고 있다면 반드시 다룬다."

> 임의의 숫자로 만든 예시였다면 나오지 않았을 발견입니다. 프롬프트 예시는 실제 데이터로 만드는 편이 낫습니다.

---

### C-11. 카테고리 지침은 하나씩 붙이고, 저평가부터 구현

**결정** — 카테고리별 지침 7종을 만들되, 런타임에는 선택된 카테고리 블록 하나만 시스템 프롬프트에 덧붙입니다. 그리고 `undervalued`(저평가)를 먼저 구현합니다.

**근거** — 7개를 다 넣으면 프롬프트가 길어지고 초점이 흐려집니다. 저평가를 먼저 하는 이유는 자동 확인이 가능한 전제가 가장 잘 나와서 코어 루프 검증에 맞고, 데모 시드 종목도 여기 배치되기 때문입니다.

---

### C-12. 데이터 모델에 가격 필드를 둘로 분리

**상황** — S5 화면을 설계하다가, 보유 종목의 기준 가격을 무엇으로 할지 문제가 나왔습니다.

**결정** — `added_price`(담은 시점)와 `avg_buy_price`(매수 단가)를 따로 둡니다.

**근거** — 근거가 아직 유효한지는 담은 날 가격 기준으로 판정해야 하고, 실제 손익은 매수가 기준입니다. 담은 시점과 산 시점이 다를 수 있어 하나로는 둘 다 표현할 수 없습니다.

화면에서도 지표를 나눠 표시합니다. "근거 대비 -4.6% / 손익 -0.9%"

---

## D. 프로세스

### D-1. 코딩 전에 디자인 단계를 넣음

**결정** — 구현 시작 전에 디자인 토큰과 핵심 화면 2장(S2 입력, S3 검토 결과)을 먼저 잡았습니다. 2시간 타임박스.

**근거** — 두 가지입니다.

첫째, 바로 코딩으로 가면 결과물이 shadcn 기본 룩으로 나와 다른 프로토타입과 구분이 되지 않습니다. 둘째, Invest Diary에서 같은 실수를 이미 했습니다. 디자인 가이드 없이 시작해서 나중에 어색한 부분을 떠안았고, 그걸 포트폴리오에 '앞으로의 과제'로 적어뒀습니다. 같은 실수를 반복하지 않기로 했습니다.

전체 화면이 아니라 2장만 한 이유는, 나머지는 여기서 잡은 토큰을 물려받으면 통일감이 유지되기 때문입니다.

**기록 보완** — 도구는 두 가지였습니다. 디자인 토큰은 shadcn 프리셋(`b3e3Rtow7k`)을 골라 손봐서 잡았고(Day 1 밤 채택, ADR-0001), 화면 2장의 와이어프레임은 Day 2(8월 26일) 오전에 Claude Design으로 그렸습니다. 커밋으로 보면 프리셋 채택(8월 25일 23:29)과 UI 기반 작업 시작(8월 26일 12:06, #28) 사이가 이 단계입니다. 소요 시간은 따로 재지 않았습니다. 그려보고 발견한 어색한 지점 세 가지와 그에 따른 구성 변경은 B-6에 있습니다. 검색 화면 신설, 관심종목·보유중 분리, 배지 3상태입니다. 프리셋을 적용하는 방법 자체는 나중에 ADR-0014로 정리했습니다.

---

### D-2. 사용자 테스트를 앞당김

**상황** — Day 1 저녁에 저장소·DB·배포가 끝나 원래 Day 2 항목의 절반이 끝났습니다. 하루 가까운 여유가 생겼습니다.

**결정** — 여유를 기능 추가가 아니라 사용자 테스트를 앞당기는 데 씁니다. Day 3 저녁 → Day 2 저녁.

**근거** — 테스트 자체보다 반영할 시간을 확보하는 게 중요합니다. Day 3에 테스트하면 반영은 Day 4 오전 몇 시간뿐입니다. "피드백을 받았다"와 "피드백으로 무엇을 바꿨다"는 다르게 읽힙니다.

S1~S3만 돌아도 가장 중요한 가설(사람들이 부담 없이 근거를 쓰는가)은 판정됩니다.

**기록 보완** — 계획대로 되지는 않았습니다. Day 2 저녁은 사용자 테스트 대신 한국어 깨짐(E-1)을 잡는 데 썼고, 구조화 출력 전환 커밋이 8월 27일 00:36에 들어갔습니다. 실제 테스트는 Day 3(8월 27일) 오전이었습니다. 지인들에게 배포 링크를 보내 S1~S3를 직접 써보게 하고 의견을 받았습니다. 나온 피드백은 세 갈래였습니다.

1. S2 첫 화면이 왜 이유를 묻는지 말하지 않는다. 처음 온 사람은 답하면 무엇을 얻는지 모른 채 카테고리를 고른다
2. 근거를 쓰지 않고 종목만 담을 길이 없다
3. "3개월 후 보기" 토글이 원래 있던 기능처럼 보이고, 변동 배너가 자리를 계속 차지한다

반영은 같은 날 안에 끝냈습니다. 1·2번은 #96(안내 문구 추가, Step 1 `건너뛰기` 신설, 15:40 머지)으로, 3번은 #103(`[데모용]` 뱃지 상시 노출, 배너를 토스트로, 16:32 머지)으로 들어갔고, 각각 E-7과 E-5의 판단으로 이어졌습니다. 앞당긴 하루가 결국 반영 시간으로 쓰였으니 결정의 취지는 지켜진 셈입니다.

---

### D-3. 시드 종목 중 하나는 비워두기

**결정** — 새 세션에 시드 종목 3개를 넣되, 둘은 근거가 작성된 상태로, 하나는 비워둡니다.

**근거** — 시드가 전부 채워져 있으면 심사자가 직접 입력해볼 이유가 없어집니다. 코어 루프를 직접 통과해봐야 제품이 이해됩니다.

---

### D-4. 데모 데이터임을 감추지 않기

**결정** — 전제 붕괴는 원래 수 주에서 수 개월이 걸리므로 "3개월 후로 이동" 토글을 제공합니다. 다만 화면 상단에 시드 종목의 미래 데이터가 시연용 가상 값임을 밝히는 배너를 고정합니다.

**근거** — 만든 데이터임을 감추면 신뢰를 잃습니다. 밝히면 설계 의도로 읽힙니다.

**이후** — 사용자 테스트에서 배너가 자리를 차지한다는 의견이 나와 표현을 바꿨습니다(E-5). 감추지 않는다는 원칙은 그대로이고, 토글 옆 `[데모용]` 뱃지가 그 역할을 맡습니다.

---

### D-5. 과제 문서를 저장소에 올리지 않음

**결정** — 과제 안내 문서는 커밋하지 않습니다. 저장소 이름과 README에도 회사명이나 채용 과제라는 표현을 넣지 않습니다.

**근거** — 과제 안내 문서에 외부 유출·무단 공유 금지가 명시되어 있습니다. 공개 저장소를 제출하는 이상 첫 커밋부터 지켜야 할 선입니다.

---

### D-6. 문서를 역할별로 쪼갬

**상황** — 기술 스펙 하나에 아키텍처, 데이터 모델, 화면 명세, 구현 순서가 다 들어 있었습니다. 화면 구성이 자주 바뀌면서 문서 전체를 계속 건드리게 됐습니다.

**결정** — 갱신 주기가 다른 것을 분리했습니다.

| 문서 | 담는 것 | 갱신 빈도 |
|---|---|---|
| PRD | 제품 논리, 가설, 지표 | 거의 없음 |
| 기술스펙 | 아키텍처, 데이터 모델, 외부 연동, 보안 | 낮음 |
| 화면명세 | 화면 구성, UI 문구, 카테고리별 질문 | 높음 |
| 프롬프트 명세 | 시스템 프롬프트, 예시 | 중간 |
| decisions.md | 판단 기록 | 계속 |

구현 순서와 일정은 기술 스펙에서 떼어내 실행계획 문서로 보냈습니다. 스펙은 "무엇을 어떻게 만드는가"만 답하게 했습니다.

---

## E. 구현 중 결정

구현하면서 그때그때 적은 판단입니다. 이슈 번호와 ADR 번호는 이 저장소의 것입니다. AI가 제안한 방향을 뒤집은 사례, 막혔다가 접근을 바꾼 사례, 만들다 보니 스펙이 틀렸다고 판단해 스펙을 고친 사례를 남기려 했습니다.

---

### E-1. 강제 tool-call을 버리고 구조화 출력으로 (#58, ADR-0003)

**상황** — LLM 응답을 JSON으로 받기 위해 처음에는 강제 tool-call을 썼습니다. 스키마를 도구 정의에 넣고 반드시 그 도구를 호출하게 하는 방식입니다.

**실측** — 출력을 실제로 세어보니 한국어 문장의 41.8%가 깨졌습니다(sonnet-5 기준, 79문장 중 33). `premises`가 통째로 빠지는 경우도 있었습니다.

이 필드들은 가공 없이 그대로 화면에 나갑니다. 깨진 한국어가 사용자에게 그대로 보인다는 뜻입니다.

**원인을 가른 실험** — 같은 모델, 같은 시스템 프롬프트로 tool 강제 없이 평문으로 시키면 깨끗하게 나왔습니다. 인코딩 문제도, 모델의 한국어 능력 문제도 아니고 강제 tool-call 경로 자체의 문제였습니다. 그다음 출력 방식 3종과 모델 2종을 비교했습니다.

| 출력 방식 | 모델 | 깨짐률 |
|---|---|---|
| 강제 tool-call | sonnet-5 | 41.8% |
| 강제 tool-call | opus-5 | 5.8% |
| tool-call + `strict: true` | opus-5 | 20.5% (구조까지 깨짐) |
| 구조화 출력 | opus-5 | 0% |
| 구조화 출력 | sonnet-5 | 0% |

`temperature` 하향은 현행 모델에서 파라미터가 없어 불가능했고, `effort` 조정은 깨짐률과 뚜렷한 관계가 없었습니다.

**결정** — `output_config.format` 기반 구조화 출력으로 바꿨습니다. few-shot의 assistant 턴도 `tool_use` 블록이 아니라 JSON을 담은 텍스트로 바꿨습니다. 모델은 sonnet-5를 유지했습니다. 구조화 출력으로 깨짐이 0이 되어 모델을 올릴 이유가 없어졌고, opus-5는 호출당 비용이 약 3배였습니다.

**부수 결정** — 출력 스키마에서 모든 키를 필수로 두고, 값이 없는 자리는 키를 빼는 대신 `null`로 채우게 했습니다. 키 생략을 허용하면 "모델이 안 냈다"와 "해당 없음"이 구분되지 않습니다. `premises` 누락의 원인이 `max_tokens` 절삭이었다는 것도 이때 드러나 상한을 올렸습니다.

🧑 출력 품질을 세어보기로 한 것, 원인을 가르는 대조 실험을 먼저 돌린 것, 전환 판단, 모델을 올리지 않기로 한 판단
🤖 초기 tool-call 구현, 조건별 실측 실행

> 이 프로젝트에서 가장 의미 있는 기록이라고 생각합니다. AI 출력을 그대로 믿지 않고 세어봤고, 숫자가 나왔고, 그 숫자 때문에 구조를 바꿨습니다. 41.8%는 눈으로 훑어봐서는 나오지 않는 값입니다. 이 실험을 하면서 유상 호출을 낭비하지 않는 규약(`docs/agents/llm-experiments.md`)도 같이 만들었습니다.

---

### E-2. 자동 전제를 저장하지 않고 조회할 때마다 계산 (ADR-0004)

**상황** — 처음 설계에서는 전제 판정 결과를 `premises.status`와 `broken_at`에 저장하고, Cron으로 하루 한 번 배치를 돌릴 계획이었습니다.

**문제 발견** — 데모 시점이 판정의 입력이라는 것을 구현하면서 알았습니다. 저장하면 `현재` 판정과 `3개월 후` 판정이 같은 행을 서로 덮어씁니다. 마지막으로 누가 어느 시점을 조회했느냐에 따라 화면이 달라집니다.

**결정** — `price`/`valuation` 전제는 저장하지 않고 조회할 때마다 계산합니다.

- 시세와 기준값만 있으면 정해지는 순수 계산이라 저장할 이유가 없습니다
- 배치도 Cron도 판정 트리거도 두지 않습니다. 실행 주기라는 개념 자체가 없어졌습니다
- `fundamental`/`qualitative`는 반대입니다. 자동 판정 대상이 아니므로 저장된 값을 읽습니다

같은 `Premise` 안에서 status의 출처가 둘로 갈리지만, 그 구분은 조회 계층 안에 머뭅니다. 배지를 그리는 쪽은 합쳐진 결과만 봅니다.

**연쇄 결정** — `sessions.demo_offset_days` 컬럼을 삭제했습니다. 데모 시점은 클라이언트가 들고 조회 인자로 넘깁니다. 서버는 사용자가 어느 시점을 보고 있는지 기억하지 않습니다.

> 스펙에 있던 Cron 항목이 통째로 없어졌습니다. 기능을 뺀 게 아니라 필요 없어진 것입니다. 저장하지 않기로 하니 돌릴 것이 없어졌습니다.

---

### E-3. PER/PBR은 재무비율 API가 아니라 현재가 응답에 있었다 (#13)

**상황** — 스펙에서는 밸류에이션 전제 판정에 재무비율 API(`/finance/financial-ratio`)를 쓴다고 적어두었습니다.

**실측** — 실제로 호출해보니 PER/PBR은 현재가 조회(`inquire-price`) 응답에 실려 옵니다. 재무비율 API는 `eps`/`bps`/`roe_val` 등 현재가에 없는 필드를 담고 있습니다.

**결과** — 밸류에이션 판정에 호출이 한 번이면 충분해졌습니다. 목록 화면에서 종목마다 두 번 부르던 것이 한 번으로 줄어, rate limit 부담도 같이 줄었습니다.

검증 기록은 `docs/research/kis-financial-ratio.md`에 남겼습니다.

> 문서에 적힌 가정을 실제 호출로 확인한 사례입니다. 확인하지 않았다면 필요 없는 API를 하나 더 붙인 채로 갔을 것입니다.

---

### E-4. 지표가 없는 종목 — 검증이 아니라 경고로 (#111)

**상황** — PER이 `정보 없음`으로 오는 종목이 있습니다(적자 기업 등). 모델이 그 지표로 `valuation` 전제를 만들면 판정할 수 없는 전제가 됩니다.

**첫 접근** — 의미 검증(`findSemanticProblems`)에 넣어서 걸리면 재시도하게 하려 했습니다.

**문제** — 그 함수의 반환값은 곧 재시도이고, 재시도 실패는 `LLMError`입니다. PER 없는 종목에서 모델이 고집을 부리면 근거 쓰기 자체가 실패합니다. 사용자는 아무 잘못도 하지 않았는데 화면이 실패합니다.

**결정** — 검증에서 빼고 경고 로그만 남깁니다.

**함께 한 것** — 프롬프트 쪽에서 막습니다. 카테고리 지침에 "사용자가 답한 지표가 `정보 없음`이면 그 지표로는 만들지 않되, 살아 있는 다른 지표가 있으면 그쪽으로 만든다"를 넣고, few-shot에 그 상황의 정답(PBR 기준 전제)을 보여주는 변형 예시를 추가했습니다.

> 검증을 강하게 걸면 안전해 보이지만, 실패의 대가를 사용자가 치르는 구조가 되기 쉽습니다. 여기서는 막는 자리를 검증에서 프롬프트로 옮겼습니다.

---

### E-5. 사용자 테스트 반영 — 상주 배너를 토스트로 (#103)

**UT에서 나온 것**

- 헤더에 상주하던 변동 배너가 "계속 자리를 차지한다"는 의견
- 배너가 첫 `달라짐` 종목의 S5로 가는 링크였는데, 그렇게 간다는 표시가 화면에 없었습니다
- 데모 토글이 Off일 때 라벨만 보여서, 원래 있던 기능처럼 읽혔습니다

**결정**

- 변동 요약을 4초짜리 토스트로 바꿨습니다. `달라짐` 종목 수가 0에서 N으로 올라가는 순간에만 지나갑니다. 탭할 수 없습니다
- 요약이 맡는 일을 주의 환기로 한정하고, S5로 가는 길은 카드 배지가 그대로 맡습니다. 5장짜리 목록에서 어디가 달라졌는지는 한눈에 들어옵니다
- 데모 토글의 `[데모용]` 뱃지를 On/Off와 무관하게 항상 노출합니다

**남긴 것** — "N개 종목은 시세를 불러오지 못해 확인할 수 없어요"는 헤더의 정적 텍스트로 유지했습니다(#81). 그건 사건이 아니라 풀리기 전까지 계속 참인 상태이고, 없애면 "왜 이 카드엔 배지가 없지"에 답할 문장이 화면에서 없어집니다.

> 사건과 상태를 구분해서 각각 다른 표현을 준 것이 이 결정의 핵심입니다. 둘 다 배너로 처리하면 하나는 어색해집니다.

---

### E-6. 주문 버튼을 `disabled`로 두지 않음 (ADR-0009)

**상황** — 프로토타입에서 실제 주문은 나가지 않습니다(`trade-client.ts`에 주문 메서드가 없습니다). `구매` 버튼을 어떻게 처리할지 정해야 했습니다.

**기각한 안** — 버튼을 `disabled`로 두는 것. 데모에서 "이 제품은 매수를 안 하는 제품"으로 읽혀 PRD의 논지를 화면이 스스로 부정합니다.

**결정** — 버튼은 살려두고, 누르면 "프로토타입에서는 구매를 지원하지 않아요" 토스트를 띄우고 시트는 열린 채로 둡니다. 홈으로 돌려보내면 아무 일도 없었는데 화면이 체결된 것처럼 보입니다.

**중요** — `order_events`는 그대로 남깁니다. `proceed`/`adjust`가 기록되지 않으면 가설 2 검증이 통째로 없어집니다.

> 실제와 다른 지점은 "실제로 주문이 나가는 순간" 한 곳이고, 그 앞의 화면 전환은 전부 실제입니다. 그 한 곳만 막았습니다.

---

### E-7. 같은 말이 한 화면에서 두 뜻을 갖지 않게 (#96)

**상황** — Step 2의 각 질문 옆에 `건너뛰기` 버튼이 있었습니다. 선택지 칩을 해제할 방법이 없어서 생긴 우회로였습니다.

**문제** — Step 1에도 `건너뛰기`가 있습니다. 그쪽은 "근거 없이 종목만 담기"라는 다른 뜻입니다. 같은 단어가 한 흐름 안에서 두 가지를 가리키고 있었습니다.

**결정**

- 선택지 칩을 토글로 만들어 다시 누르면 해제되게 했습니다
- 질문별 `건너뛰기` 버튼을 없애고, 답하지 않은 질문은 무선택으로 표현합니다
- 무선택 문항은 저장될 때 `skipped`로 채워지므로 "무엇을 비워뒀는지"는 그대로 남습니다
- 하단 `다음`은 최소 한 문항을 답해야 열립니다. 아무것도 쓰지 않겠다는 선택은 Step 1의 `건너뛰기`가 받습니다

> UI 요소를 늘려서 푼 문제(건너뛰기 버튼)가 사실은 다른 문제(칩 해제 불가)의 증상이었습니다. 원인을 고치니 요소가 하나 줄었습니다.

---

### E-8. 재시도의 경계를 하나로 정리 (#111, #114, #119, #120 → ADR-0013)

**상황** — E-4 이후로 같은 자리에 세 번 더 섰습니다. few-shot 예시 때문에 `is_challengeable`이 `true`로 치우치는 것(#114), 화면 문구에 금지어 "전제"가 새는 것(103콜 중 4건, #119), `check_config`가 객체째 `null`로 오는 것(103행 중 25행, #120). 볼 때마다 "그럼 검증에 넣자"가 자연스러운 다음 수처럼 보였습니다.

**문제** — 검증에 규칙 하나를 넣는 것은 "이 규칙을 두 번 어기면 사용자가 근거를 쓰지 못한다"고 정하는 것과 같습니다. 재시도는 유상이고, 세션당 20회 상한을 한 칸 더 쓰며, 분포 편향(#114)처럼 응답 하나로는 판정할 수 없는 위반도 있습니다.

**결정** — 네 번 따로 내린 같은 답을 규칙 하나로 정리했습니다. 재시도는 "이 응답을 그대로 쓰면 제품이 틀린 판정을 한다"일 때만 겁니다. 그 외의 위반은 통과시키고 경고 로그로 남기며, 로그는 다음 프롬프트 튜닝의 재료로 씁니다. 스키마를 느슨하게 둔 자리에는 그것이 의도임을 주석으로 남깁니다.

🧑 네 번 같은 자리에 섰다는 것을 알아채고 규칙으로 올린 판단, 재시도를 "이유가 있어야 하는 선택"으로 바꾼 것
↩️ 네 이슈 모두에서 AI가 처음 제안한 "검증에 추가"를 뒤집었습니다

> 관측된 위반마다 규칙을 하나씩 붙이면 안전해지는 것이 아니라 실패하는 경우가 늘어납니다. 이 저장소에서 재시도는 기본값이 아닙니다.

---

### E-9. 초안 하나는 유상 호출을 최대 한 번만 만든다 (#122)

**상황** — S3(AI 검토 결과)에서 뒤로 갔다가 다시 들어오면 같은 근거로 유상 LLM 호출이 한 번 더 나갔습니다. 가드가 화면 마운트 단위여서 뒤로가기 한 번에 리셋됐습니다.

**결정** — 가드를 화면이 아니라 근거 초안 쪽에 붙였습니다. 초안은 자기가 만든 검토 결과 한 자리를 함께 들고 있어, S3에 다시 들어와도 앞서 본 결과를 다시 보여줄 뿐 호출을 새로 하지 않습니다. 담기에 성공하면 초안은 결과와 함께 사라지고, 그 뒤 S3는 초안이 아니라 저장된 근거를 읽습니다. 다시 써서 들어온 초안은 새 것이므로 결과 자리도 비운 채 시작합니다.

**근거** — 규칙이 초안 쪽에 있는 이유는 수명 때문입니다. 화면에 두면 뒤로가기 한 번에 리셋되고, 세션당 20회 상한은 그만큼 줄어든 채 돌아오지 않습니다. 다시 보여줄 때 시세는 생성 시점 값 그대로 둡니다. 검토 결과와 시세는 한 쌍이고(ADR-0005와 같은 규칙), 재진입 후 `이대로 담기`를 누르면 그 값이 담은 날 가격이 됩니다.

> 가드를 어디에 둘지는 그 상태가 얼마나 오래 사는지로 정했습니다. 비용 상한이 있는 호출은 가장 오래 사는 자리에서 세야 합니다.

---

### E-10. 근거 갱신은 빈 화면에서 다시 시작한다 (#154)

**상황** — 이미 담긴 종목의 근거를 다시 쓰는 흐름(S5 `생각 업데이트 하기`, S4 주문 전 확인)에서, 이전 근거를 S2에 미리 채워 두자는 제안이 있었습니다. 입력을 아끼는 편의로 보입니다.

**결정** — 이어받지 않습니다. 옛 카테고리도, 후속 질문 답도, 자유 서술도 채워 넣지 않고 빈 화면에서 시작합니다.

**근거** — 근거 갱신은 편의 기능이 아니라 다시 생각하게 하는 장치입니다. 채워진 답이 있으면 사용자는 그것을 읽고 넘기지 다시 생각하지 않습니다. 통과가 쉬워지면 거의 같은 입력에 유상 호출만 한 번 더 나가고, `order_events`의 `update_thesis`가 재는 "다시 생각했다"도 의미가 옅어집니다. 이어받기를 넣으려면 이 마찰이 비용이 아니라 기능이라는 점을 먼저 뒤집어야 합니다.

↩️ AI의 "이전 값을 채워 두자"는 제안을 뒤집었습니다

> 제품에서 마찰을 없애는 것이 늘 옳지는 않습니다. 이 제품은 마찰을 담는 순간과 다시 생각하는 순간 두 자리에만 두고, 나머지에서는 없앴습니다(B-2). 어느 자리에 마찰을 남길지가 제품의 정의라고 생각합니다.
