# 프로토타입 기술 스펙 — 「투자아이디어 검증기」

개발 착수용 문서 | 2026.08 | 손성기

> 이 문서는 제출물이 아니라 구현을 위한 작업 문서입니다.
> 제품 논리와 배경은 별도 PRD를 참조하고, 여기서는 무엇을 어떻게 만들지만 다룹니다.

---

## 0. 용어 규칙

세 층위로 나눠 씁니다. 하나의 단어로 전부 덮으려 하지 않습니다.

| 층위 | 사용 어휘 | 예시 |
|---|---|---|
| 코드 · DB | 영어 식별자 | `thesis`, `premise`, `critique` |
| 문서 | 투자 근거 / 전제 | "근거를 전제로 분해한다" |
| **UI 카피** | **개념어를 쓰지 않는다. 질문형으로.** | "왜 이 종목이 궁금하세요?" |

UI에서 "근거를 작성하세요"라고 하면 사용자가 얼어붙습니다. 물어보면 답이 나옵니다. 화면 명세의 문구 예시는 이 원칙을 따릅니다.

---

## 1. 확정된 결정사항

| 항목 | 결정 | 근거 |
|---|---|---|
| 인증 | 없음. 익명 세션(httpOnly 쿠키) | 심사자 진입 마찰 0. 세션별 데이터 격리 |
| 폼팩터 | 모바일 뷰포트 고정 (390×844), 데스크톱에서는 중앙 정렬 + 여백 | 입력 설계의 전제가 모바일. MTS 맥락 유지 |
| 구현 범위 | ①②③④ 실동작, ⑤는 자동 판정 가능한 전제만 | 4일 제약. 한계를 제품 안에 드러냄 |
| 시간 시연 | 데모 시나리오 모드 ("3개월 후로 이동") | 붕괴 감지를 동작으로 증명 |
| 증권사 API | 한국투자증권 **모의투자** | 실계좌 노출 없음. 확보 완료 |
| LLM | Anthropic API (Claude) | 기존 작업 환경. structured output 사용 |
| 종목 범위 | 국내주식, 데모용 20~30종목 화이트리스트 | 재무 데이터 사전 캐싱으로 응답 속도 확보 |
| 알림 | 인앱 배지·목록만. 푸시·이메일 없음 | 4일 범위 밖 |

---

## 2. 기술 스택

```
Frontend    Next.js (App Router) + TypeScript + Tailwind + shadcn/ui
Backend     Next.js Route Handlers / Server Actions
DB          Supabase (Postgres)
외부연동     한국투자증권 모의투자 REST API
            Anthropic API (structured output)
배포        Vercel
```

Invest Diary에서 검증한 **한투 API 어댑터 계층만 이식**합니다. 나머지는 새 저장소에서 새로 작성합니다. README에 재사용 사실을 명시합니다.

---

## 3. 데이터 모델

```sql
-- 익명 세션
sessions (
  id              uuid PK,
  created_at      timestamptz,
  demo_offset_days int default 0   -- 데모 시나리오 모드용 시간 오프셋
)

-- 관심종목
watchlist_items (
  id              uuid PK,
  session_id      uuid FK,
  ticker          text,            -- 종목코드 6자리
  name            text,
  status          text,            -- watching | bought | removed
  is_seed         boolean default false,
  created_at      timestamptz
)

-- 투자 근거 (버전 관리)
theses (
  id              uuid PK,
  watchlist_item_id uuid FK,
  version         int default 1,
  category        text,            -- fundamental|undervalued|theme|dividend|technical|recommended|gut
  followup        jsonb,           -- AI 후속질문에 대한 답변들
  free_text       text,            -- 선택 입력
  created_at      timestamptz
)

-- AI 검증 결과
critiques (
  id              uuid PK,
  thesis_id       uuid FK,
  is_challengeable boolean,        -- 반박할 만한지 판정
  counterpoints   jsonb,           -- [{point, severity, basis}]
  open_questions  jsonb,
  raw_response    jsonb,           -- 원본 보관 (디버깅·문서화용)
  created_at      timestamptz
)

-- 확인 가능한 전제
premises (
  id              uuid PK,
  thesis_id       uuid FK,
  statement       text,            -- "PER 15배 이하 유지"
  check_type      text,            -- price | valuation | fundamental | qualitative
  check_config    jsonb,           -- {metric, operator, value, period}
  status          text,            -- intact | broken | pending | manual
  last_checked_at timestamptz,
  broken_at       timestamptz,
  observed_value  text
)

-- 주문 시점 행동 기록 (핵심 지표 수집)
order_events (
  id              uuid PK,
  session_id      uuid FK,
  watchlist_item_id uuid FK,
  thesis_shown    boolean,
  initial_qty     int,
  final_qty       int,
  action          text,            -- proceed | adjust | cancel | update_thesis
  created_at      timestamptz
)
```

**세션 격리 원칙** — 모든 조회 쿼리는 `session_id` 필터를 반드시 거칩니다. Supabase RLS로 강제하거나, 서버 액션에서 일괄 처리합니다. (PRD 리스크로 언급했던 권한 모델 문제를 처음부터 잡고 갑니다.)

---

## 4. 화면 명세

### S1. 관심종목 목록 (홈)

- 종목 카드: 종목명, 현재가, 등락률
- 상태 배지: `메모 있음` / `메모 없음` / `달라진 점 있음`
- 하단 CTA: `+ 종목 추가`
- 상단: 데모 시나리오 토글 `3개월 후 보기`

빈 화면 방지를 위해 새 세션에는 시드 종목 2~3개를 자동 복사합니다. 그중 하나는 이미 근거가 작성된 상태로 둡니다.

> **문구 예시**
> 빈 상태: "관심 가는 종목을 담아보세요. 왜 담았는지 같이 적어두면 나중에 도움이 됩니다."

### S2. 종목 추가 → 근거 입력

3스텝 구조입니다.

**Step 1 — 카테고리 선택 (단일 선택)**

> **문구 예시**
> 헤드라인: "왜 이 종목이 궁금하세요?"

```
실적·성장  |  저평가  |  테마·모멘텀  |  배당
기술적 신호  |  누가 추천해서  |  그냥 느낌
```

"누가 추천해서", "그냥 느낌"은 반드시 남깁니다. 선택 자체가 자각을 만듭니다.

**Step 2 — AI 후속 질문 (동적 생성, 2~3개)**

카테고리별로 첫 질문은 사전 정의하고, 이후는 LLM이 생성합니다.
선택지 + "직접 입력" 옵션을 함께 제공합니다.

> **문구 예시**
> 저평가 선택 시: "무엇 대비 싸다고 보세요?"
> → 동종업계 / 과거 이 종목의 밸류에이션 / 절대 지표 / 직접 입력

**Step 3 — 자유 서술 (선택, 200자 이내)**

> **문구 예시**
> "덧붙이고 싶은 게 있다면 짧게 적어주세요. (건너뛰어도 됩니다)"

### S3. AI 검증 결과

- **반박할 지점이 있을 때**: 반대 시각 2~3개 + 확인할 질문
- **반박할 지점이 약할 때**: 압박 UI를 띄우지 않고 확인 지표만 제시

무조건 반박하지 않는다는 원칙(PRD 0장)을 여기서 구현합니다. `is_challengeable=false`이면 반대 시각 섹션 자체를 렌더하지 않습니다.

> **문구 예시**
> 반박 있음: "이런 점은 어떻게 보세요?"
> 반박 없음: "생각이 구체적이네요. 대신 이건 지켜볼 만합니다."
> 전제 섹션: "앞으로 이걸 지켜보면 됩니다" + 각 항목에 `자동 확인` / `직접 확인` 배지

- 액션: `다시 쓰기` / `이대로 담기`

### S4. 매수 주문 (④)

- 수량·가격 입력 (모의투자)
- **근거가 있으면** 상단 카드로 노출, **없으면** 아무것도 뜨지 않고 바로 주문 화면

> **문구 예시**
> "3주 전에 이렇게 생각하셨어요"
> (요약 + 전제 상태)
> → `그대로 진행` / `수량 조정` / `다시 생각하기` / `취소`

모든 분기에서 `order_events`를 기록합니다. 핵심 지표가 여기서 나옵니다.

### S5. 전제 추적 / 알림

- 전제별 상태 목록 (유지 / 달라짐 / 직접 확인 필요)
- 붕괴 시 기준값과 관측값을 나란히 보여줍니다

> **문구 예시**
> "담으실 때 보신 것과 달라진 게 있어요"
> "PER 15배 이하 → 지금은 21.3배"
> → `생각 업데이트` / `매도 검토` / `그대로 두기`

---

## 5. 외부 연동

### 5-1. 한국투자증권 모의투자 API

| 용도 | 엔드포인트 | 비고 |
|---|---|---|
| 접근토큰 발급 | `/oauth2/tokenP` | 유효기간 24h, **반드시 캐싱**. 발급 호출 제한 있음 |
| 현재가 시세 | `/uapi/domestic-stock/v1/quotations/inquire-price` | 가격 기반 전제 판정 |
| 재무비율 | `/uapi/domestic-stock/v1/finance/financial-ratio` | PER/PBR 등 밸류에이션 전제 판정 |
| 잔고 조회 | `/uapi/domestic-stock/v1/trading/inquire-balance` | 모의투자용 TR_ID 별도 |
| 현금 주문 | `/uapi/domestic-stock/v1/trading/order-cash` | 모의투자용 TR_ID 별도 |

> **TR_ID는 실전과 모의가 다릅니다.** 개발자센터에서 모의투자용 코드를 확인해 상수로 분리하세요.
> Rate limit이 있으므로 목록 화면에서 종목별 개별 호출 대신 배치 + 캐싱(30~60초)으로 처리합니다.

### 5-2. Anthropic API

**호출은 서버에서만.** 클라이언트 번들에 키가 들어가면 안 됩니다.

**호출 1회로 통합** — 검증과 전제 추출을 한 번에 받습니다. 라운드트립을 줄여 체감 속도를 확보합니다.

입력: 종목명·업종, 카테고리, 후속질문 답변, 자유 서술, 종목 기본 지표(현재가·PER·PBR)

출력 스키마:

```json
{
  "is_challengeable": true,
  "challenge_reason": "'실적이 좋다'에 머물러 있고 밸류에이션 판단이 없음",
  "counterpoints": [
    { "point": "...", "severity": "major|minor", "basis": "..." }
  ],
  "open_questions": ["..."],
  "premises": [
    {
      "statement": "PER 15배 이하 유지",
      "check_type": "valuation",
      "check_config": { "metric": "per", "operator": "lte", "value": 15 }
    },
    {
      "statement": "경쟁사가 이 시장에 진입하지 않는다",
      "check_type": "qualitative",
      "check_config": { "remind_after_days": 90 }
    }
  ]
}
```

**프롬프트 원칙 (시스템 프롬프트에 명시)**

1. 종목을 추천하거나 매수·매도를 권유하지 않는다
2. 사용자가 쓴 근거의 내부 정합성만 다룬다
3. 반박할 지점이 약하면 억지로 반박하지 않고 `is_challengeable: false`를 반환한다
4. 전제는 반드시 관측 가능한 형태로 변환한다. 불가능하면 `qualitative`로 분류한다
5. 출력 문장은 "논지", "가설" 같은 개념어 대신 일상어를 쓴다

**안전장치** — 세션당 LLM 호출 상한(예: 20회), 요청 타임아웃, 실패 시 재시도 1회.

---

## 6. 전제 판정 엔진

`check_type`에 따라 판정기를 분기합니다.

| check_type | 판정 방법 | 4일 내 구현 |
|---|---|---|
| `price` | 현재가 vs 기준값 비교 | ✅ |
| `valuation` | 재무비율 API의 PER/PBR vs 기준값 | ✅ |
| `fundamental` | 분기 실적 조회 후 비교 | △ 데모 시나리오로 대체 |
| `qualitative` | 자동 판정 불가. `manual` 상태로 리마인드만 | ✅ (판정 안 함) |

**실행 주기** — Vercel Cron으로 하루 1회. 데모에서는 시나리오 모드가 대신하므로 필수는 아닙니다. 시간이 없으면 화면 진입 시 on-demand 판정으로 대체해도 무방합니다.

**판정 결과 기록** — `premises.status`를 갱신하고 `broken_at`, `observed_value`를 남깁니다. S5에서 "기준 PER 15배 → 현재 21.3배"처럼 근거를 보여주기 위한 데이터입니다.

---

## 7. 데모 시나리오 모드

붕괴 감지는 원래 수 주~수 개월이 걸립니다. 심사자가 30초 안에 코어 가치를 체험하도록 시간 이동을 제공합니다.

**동작**

- 상단 토글: `현재` ↔ `3개월 후`
- `3개월 후` 선택 시 `sessions.demo_offset_days = 90`
- 시드 종목에 한해 미리 준비한 시나리오 데이터(가격·PER 변화)를 사용
- 사용자가 직접 추가한 종목은 실제 데이터를 그대로 사용

**정직성 확보** — 화면 상단에 배너를 고정합니다.

> "데모 모드입니다. 시드 종목의 3개월 후 데이터는 시연을 위해 준비한 가상 값입니다."

조작된 데이터임을 감추면 신뢰를 잃습니다. 명시하면 오히려 설계 의도로 읽힙니다.

**시나리오 구성 (최소 2개)**

1. **전제가 깨지는 경우** — "PER 15배 이하" 전제 → 실제로 21배로 상승 → 알림
2. **전제가 유지되는 경우** — 생각이 여전히 유효함을 확인해주는 화면. 알림이 항상 나쁜 소식만은 아니라는 것을 보여줍니다

---

## 8. 보안 체크리스트

- [ ] `.gitignore`와 `.env.example`을 **첫 커밋에** 포함
- [ ] 한투 API 키·계좌번호, Anthropic 키는 서버 환경변수로만. 커밋 히스토리에 절대 유입 금지
- [ ] LLM·한투 호출은 Route Handler / Server Action에서만
- [ ] 모든 조회에 `session_id` 필터 강제 (RLS 또는 서버 레이어)
- [ ] LLM 호출 상한 및 rate limit
- [ ] 실계좌 아닌 **모의투자** 사용을 README와 화면에 명시

---

## 9. 구현 순서

### Day 2 — 뼈대와 코어 루프 전반

1. 저장소 생성, `.gitignore`·`.env.example` 커밋, Vercel 배포 파이프라인
2. Supabase 스키마 적용, 익명 세션 미들웨어
3. 한투 어댑터 이식 → 시세 조회 1건 성공 확인
4. S1(목록), S2(근거 입력 3스텝) UI
5. LLM 통합 호출 → S3(검증 결과)까지 한 바퀴

**체크포인트: 종목 추가 → 입력 → AI 검증 → 저장이 끝까지 돈다**

### Day 3 — 연동과 검증

1. S4(매수 주문 + 리마인드), `order_events` 기록
2. 전제 판정 엔진 (price / valuation)
3. S5(추적 화면), 데모 시나리오 모드
4. 시드 데이터 구성
5. **저녁: 사용자 테스트 3~5명**

### Day 4 — 반영과 문서

1. 오전: 피드백 반영. **기능 추가 금지**, 있는 것만 다듬기
2. 오후: PRD 축약(1~2p), 구현 과정 문서, `decisions.md` 정리
3. 배포 확인, 제출
4. 저녁 2시간은 사고 대비로 비워둘 것

---

## 10. 잘라낼 순서 (시간 부족 시)

위에서부터 버립니다.

1. 데모 시나리오 모드 → S5를 정적 화면으로 대체
2. `valuation` 판정 → `price` 판정만
3. Cron 배치 → 화면 진입 시 on-demand 판정
4. S4 실제 주문 → 주문 직전 화면까지만
5. AI 후속 질문 동적 생성 → 카테고리별 고정 질문

**절대 지킬 것** — ①②③ 코어 루프, 세션 격리, 보안 체크리스트, `decisions.md` 실시간 기록.

---

## 11. 열린 항목

- 모의투자 TR_ID를 개발자센터에서 확인해 상수 파일로 분리
- 재무비율 API로 PER/PBR이 실제로 반환되는지 종목 1건으로 사전 검증 (밸류에이션 전제 사용 가능 여부가 여기서 갈림)
- 데모 화이트리스트 종목 선정 — 업종이 겹치지 않게 20~30종
- 제품명 확정
